# User Requests - 2026-01-26

1. The sort function in the source panel only works for the first folder. Make it working with any selected folder.
2. Push bug fix to repo.
3. If I select one of the existing videos in the destinations panel, it should play in the video panel.
4. Rename the label of the player_layout with the name of the file being played.
5. Move the "Move video" button from destinations panel to video buttons. Put it on the right, after the volume slider. Add a behaviour to the move function so that the player automatically switch to the next source video, and starts playing it (unless it is the last one).
6. In the video panel, group all buttons except the move video button, and put them in the center. If the video player is set to full screen, only keep those buttons, and mask the move video button.
7. The volume slider must not be separated from the video controls anymore. Furthermore, it must also be displayed in full screen mode.
8. Change the "Move video" button label to "Archive >>".
9. Add a new "<< Unarchive" button in the video panel, on the bottom left of the player. This button wont show up if video is fullscreen. The expected behaviour is to take the selected video from the destination panel (if any), and move it back to the selected source folder. The newly moved video will be automatically selected. As for the move button, if the file already exists, the user will be prompted for what to do.
10. Place the unarchive button to the very right of the player panel.
11. Oops, I meant the left, not the right.
12. We are done for today. Send the result to the repo.
13. Can you create a new file in the project, called "Prompts", writing all that I asked today ?
